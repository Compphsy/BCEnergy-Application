//SPDX-License-Identifier: MIT
pragma solidity ^0.8;


// The energy trasnfer contract(ETC) will accept an energy quantity from a valid registered address.
// The ETC will ascertain if consumer or producer
// The ETC will check the current price for producer or consumer
// The ETC will hold the time of contract creation
// When energy quantity changes the contract is finalised 
// A new contract is then made with the new quantity 


contract energyTransferContract {
    
    // A contract that itemises procumers energy transactions. 


    //******************************** Local variables to the contract*****************************************
    
    // enum to hold the seller details
    // variable to hold the municipal power source address
    // variable to hold the local subtation ID of seller
    // enum to hold the buyers details
    // array to hold the local auctioneers
    // variable to hold the final amount
    // variable to hold the contract start time
    // enum of contract details sent to Transfer FundsContract

    //******************************** Consructor for the contract*********************************************

    // Constructor to create the auction
    
    //*************************Modifier function that checks the valdity of a user*****************************


    // modifier that checks if the message sender is the owner of the contract

    //*************************Getter and setter functions*****************************


}